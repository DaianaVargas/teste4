<!-- <%= JSON.stringify(categorias) %> -->

<!-- <% Object.keys(categorias).forEach(key => { %>
    <%= key %><br />
<% }) %> -->

<!-- <% categorias.forEach(categoria => { %>
    <%= categoria.categoria %> 
    <a href="/categorias/editar/<%= categoria.id %>">Editar</a>
    <a href="/categorias/excluir/<%= categoria.id %>">Excluir</a><br />
<% }) %> -->


<%- include('../header') %>

<div class="container">
  <div class="alert alert-primary" role="alert">
    Categorias
  </div>
  <!-- Content here -->
  <a class="btn btn-light" style="margin-top:10px" href="/categorias/nova">Nova categoria</a>  
  <a class="btn btn-light" style="margin-top:10px" href="/publicacoes/nova">Nova publicação</a> 
  <br/> <br/>
  <!-- <table class="table table-hover table-dark"> -->
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th scope="col">Categoria</th>
        <th scope="col">Ações</th>
      </tr>
    </thead>
    <tbody>
      <% categorias.forEach(categoria => { %>
      <tr>
        <td>
          <!--< %= categoria.categoria %></td> -->
          <a href="/publicacoes/categoria/<%= categoria.id %>">
            <%= categoria.categoria %>
          </a>
        <td>
            <a href="/categorias/editar/<%= categoria.id %>">Editar</a>
            <a href="/categorias/excluir/<%= categoria.id %>">Excluir</a>
        </td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</div>


<%- include('../footer') %>
