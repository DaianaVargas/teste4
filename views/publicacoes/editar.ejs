<!-- <form method="POST" action="/publicacoes/editar/<%= publicacao.id %>">
    <input placeholder="Insira o título" type="text" name="publicacaoTitulo" value="<%= publicacao.publicacao %>" />
    <textarea placeholder="Insira a publicacao" name="publicacaoArea" value="<%= publicacao.publicacao %>"></textarea>
    <button>Salvar</button>
</form> -->

<%- include('../header') %>

<div class="container">
    <!-- Content here -->
    <div class="alert alert-primary" role="alert">
        Editar Publicação
    </div>
    <form method="POST" style="margin-top:10px" action="/publicacoes/editar/<%= categoria %>/<%= publicacao.id %>">
        <!-- <div class="form-group">
            <label for="exampleInputEmail1">Publicação</label>
            <input type="text" class="form-control" id="publicacaoTitulo" name="publicacaoTitulo" placeholder="Digite a publicacao" value="<%= publicacao.titulo %>">        
            <textarea placeholder="Insira a publicacao" name="publicacaoArea" value="<%= publicacao.conteúdo %>"></textarea>
        </div>   -->
        <div class="form-group">
            <label for="titulo">Título</label>
            <input type="text" class="form-control" id="titulo" name="titulo" placeholder="Digite o título" value="<%= publicacao.titulo %>">        
        </div>  
        <div class="form-group">
            <label for="conteudo">Conteúdo</label>
            <textarea class="form-control" id="conteudo" name="conteudo" placeholder="Digite o conteúdo"><%= publicacao.conteudo %></textarea>
        </div>  
        <button type="submit" class="btn btn-primary">Salvar</button>
    </form>
</div>


<%- include('../footer') %>